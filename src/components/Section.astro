---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="section">
  <h2>{title}</h2>

  <div class="section-content">
    <slot />
  </div>
</section>